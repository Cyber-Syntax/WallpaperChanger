#!/usr/bin/python3
"""Entry point for WallpaperChanger application.

This script serves as the main entry point for running the wallpaper changer,
which sets wallpapers based on the day of the week and monitor setup.

Example:
    $ python main.py

"""

from src.cli import run

if __name__ == "__main__":
    run()
